#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; c++)
		putchar(ch);

	for (ch = 'A'; ch <= 'Z'; c++)
		putchar(ch);

	putchar('\n');

	return (0);
}
